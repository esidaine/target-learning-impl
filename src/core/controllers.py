# write two separate classes (or functions) that do the exact same job, but in different ways:
# BackpropController: Uses PyTorch's autograd to do gradient descent on the neuron activations to find c∗.
# PIDController: Uses your biological, dynamical systems math to find c∗






# Weights & Biases (wandb.ai).
# Instead of manually trying to draw matplotlib graphs of your network's error rate, wandb is a free library you import that silently watches your network train and builds a live, beautiful, interactive dashboard in your web browser. You can literally watch your Target Learning PID controller drop the error rate in real-time. It is the absolute gold standard for ML research papers today.