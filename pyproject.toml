[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-v --tb=short"
log_cli = true
log_cli_format = "%(asctime)s [%(levelname)s] %(name)s: %(message)s"
log_cli_level = "ERROR"